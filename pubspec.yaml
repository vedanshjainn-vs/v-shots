name: v_shots
description: V Shots - Music Streaming App
version: 0.1.0+1
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  youtube_explode_dart: ^2.2.2
  just_audio: ^0.9.39

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
